# Copy to .env (never commit .env). Used by pipeline when variables are set in the shell.
# See CLAUDE.md "Secrets and configuration".

LLM_API_KEY=
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=deepseek-v4-pro
# Optional: override default extraction temperature (see pipeline.config.LLMConfig)
# LLM_TEMPERATURE=0.05
